# Nuxt SSG ????

> **??**: 2025-11-01  
> **Nuxt ??**: 4.1.2  
> **??**: article ?? `_payload.json` ?????proxy-image API ????

---

## ?? ????

### ?? 1: Article ???? 404?_payload.json ???

**??**?
- ?? `npm run generate` ??`output/public/article/` ?????
- ??????? `[404] Server Error`
- ?????`queryContent is not defined`

**????**?
1. ?????? Nuxt Content API
2. ??????Nuxt Content ?????? 'content' collection
3. ??? `queryCollection('content').all()` ?? `queryContent('articles')`
4. `queryContent` ? SSG ?????????????

### ?? 2: proxy-image API ??

**????**?
- API ???????? `output/server/chunks/routes/api/`
- `_routes.json` ?????include: `/*`, ??? `/api/**`?
- Cloudflare Pages ?? API ???? Functions ??
- ????????????

---

## ? ????

### ?? Article ?????

**????**: `app/pages/article/[id].vue`

#### 1. ?? `queryCollection` API

```typescript
// ? ???????? queryContent is not defined?
const { data: currentArticle } = await useAsyncData(
  `article-${articleId}`,
  () => queryContent(`/articles/${articleId}`).findOne(),
  { server: true, lazy: false }
)

// ? ??????? queryCollection?
const { data: allArticles, error: articlesError } = await useAsyncData(
  'articles-list',
  () => queryCollection('content').all(),
  { server: true, lazy: false }
)

// ????????????
const currentArticle = computed(() => {
  if (!allArticles.value) return null
  return allArticles.value.find(item => {
    const itemId = item.stem ? item.stem.replace(/^articles\//, '') : item.id
    return itemId === articleId
  })
})
```

#### 2. ?? meta ??

Nuxt Content v3 ??? frontmatter ????? `meta` ??????? JSON ????

```typescript
const article = computed(() => {
  if (!currentArticle.value) return null
  
  const item = currentArticle.value
  // ?? meta ?????? JSON ???
  const meta = typeof item.meta === 'string' ? JSON.parse(item.meta) : item.meta || item || {}
  
  return {
    id: meta.id || articleId,
    title: meta.title || item.title,
    date: meta.date || item.date,
    // ... ????
  }
})
```

#### 3. ??????????

```typescript
const sortedArticles = computed(() => {
  if (!allArticles.value) return []

  return allArticles.value
    .filter(item => item.path && item.path.startsWith('/articles/'))
    .map(item => {
      const meta = typeof item.meta === 'string' ? JSON.parse(item.meta) : item.meta || item || {}
      const itemId = item.stem ? item.stem.replace(/^articles\//, '') : meta.id || item.stem
      return {
        id: itemId,
        title: meta.title || item.title,
        date: meta.date || item.date,
        thumbnail: meta.thumbnail || item.thumbnail || meta.image || item.image,
        path: item.path || item._path,
      }
    })
    .filter(item => item.id)
    .sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime())
})
```

#### 4. ?? 404 ????

```typescript
// ? ???????????? 404?
if (!article.value) {
  throw createError({ statusCode: 404, message: '?????' })
}

// ? ??????? server ????????????
if (!currentArticle.value && import.meta.server) {
  throw createError({ statusCode: 404, message: '?????' })
}
```

### ?? Nitro ??

**????**: `nuxt.config.ts`

```typescript
prerender: {
  crawlLinks: true,
  routes: prerenderRoutes,
  autoSubfolderIndex: true, // ?? true??????????? index.html
  failOnError: false,
  concurrency: 10,
}
```

---

## ?? ????

### ?? SSG ??

```bash
npm run generate
```

**????**?
```
[nitro] ? Prerendered 213 routes in 10.096 seconds
```

### ??????

```bash
# ?? article ??
ls -la output/public/article/
# ????: art-nouveau, mbe, modern-design-intro, pixel-art, pop-art, vaporwave

# ?? payload ??
find output/public/article -name "_payload.json"
# ???? 6 ? payload ??

# ??????
ls -la output/public/article/art-nouveau/
# ????: index.html, _payload.json

# ?? API Functions
ls -la output/server/chunks/routes/api/
# ????: proxy-image.get.mjs, send-email.post.mjs
```

---

## ?? ??????

| ?? | ??? | ??? |
|------|--------|--------|
| ?????? | 201 | 213 (+12) |
| Article ?? | ? ??? | ? ?? |
| Article _payload.json | ? ? | ? 6 ??? |
| ????? | ? 404 Server Error | ? ??? |
| API Functions | ? ???? | ? ???? |

---

## ?? ????

### 1. Nuxt Content API ??

??????
- ? ?? `queryCollection('content').all()` ??????
- ? ???? `queryContent('articles')` ???? undefined error?
- ???????????? content collection

### 2. Meta ????

Nuxt Content v3 ??? frontmatter ????????
- ?? `item.meta` ???? JSON ???
- ??? `item` ?????
- ?? `item.stem` ???????? ID

### 3. SSG ?????

- `autoSubfolderIndex: true` - ???????
- `server: true` - ??? SSG ???????
- `lazy: false` - ????????????

### 4. API ????

- API ???????????????
- Cloudflare Pages ????? Functions ??
- ??????????????

---

## ?? ??????

- [x] `experimental.payloadExtraction: true` ???
- [x] Article ???? `queryCollection` API
- [x] `autoSubfolderIndex: true` ???
- [x] 404 ????????? server ????
- [x] Meta ????????? JSON ?????
- [x] ?? article ????? `prerenderRoutes`
- [x] API ????? `prerender: false`
- [x] `npm run generate` ?????
- [x] Article _payload.json ?????
- [x] API Functions ????

---

## ?? ????

- [NUXT_SSG_DYNAMIC_ROUTES.md](./NUXT_SSG_DYNAMIC_ROUTES.md) - ?? SSG ????
- [Nuxt Content v3 Documentation](https://content.nuxt.com/)
- [Nitro Prerendering Guide](https://nitro.unjs.io/guide/routing/prerendering)

---

**????**: 2025-11-01
