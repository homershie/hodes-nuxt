# Nuxt SSG ????????

> **??**: HODES - Homer Shie ???????  
> **????**: 2025-10-31  
> **Nuxt ??**: 4.1.2  
> **??**: ?? SSG ??????????????????

---

## ?? ????

### ?????
- ???????`/article/*`?`/project/*`?`/blog/page/*`???? `_payload.json` ????? 404 ??
- ???????????? JSON ????
- API ???`/api/proxy-image`?? Cloudflare Pages ???????

### ????
1. Nuxt 4 ? SSG ???????? `payloadExtraction: true`
2. ?????????????? Nuxt ????????
3. Cloudflare Pages ?????????????? API ?????????

---

## ? ????

### 1. ?? Payload ????

? `nuxt.config.ts` ?????????

```typescript
experimental: {
  componentIslands: true,  // ????
  payloadExtraction: true, // ? ??????? SSG
}
```

**??????**
- Nuxt 4 ? SSG ?? `payloadExtraction` ??? `_payload.json` ??
- ?? payload ???????????????????????

---

### 2. ?????????

#### ????? `nuxt.config.ts` ??????? ?????

```typescript
// ??????
const getArticleSlugs = () => {
  try {
    const articlesDir = fileURLToPath(new URL('./content/articles', import.meta.url))
    return readdirSync(articlesDir)
      .filter(filename => filename.endsWith('.md'))
      .map(filename => filename.replace(/\.md$/, ''))
  } catch (error) {
    console.warn('[prerender] ?????????', error)
    return []
  }
}

// ??????
const resolvePortfolioRoutes = () => {
  if (!Array.isArray(portfolio) || portfolio.length === 0) {
    return []
  }
  return portfolio
    .filter(item => item && (typeof item.id === 'string' || typeof item.id === 'number'))
    .map(item => `/project/${item.id}`)
}

// ????????????
const articleSlugs = getArticleSlugs()
const articleRoutes = articleSlugs.map(slug => `/article/${slug}`)

const POSTS_PER_PAGE = 10
const blogPageTotal = Math.max(1, Math.ceil(articleSlugs.length / POSTS_PER_PAGE))
const blogPageRoutes = Array.from({ length: blogPageTotal }, (_, index) => `/blog/page/${index + 1}`)

const projectRoutes = resolvePortfolioRoutes()

const baseStaticRoutes = ['/', '/about', '/service', '/contact', '/portfolio']
const prerenderRoutes = Array.from(new Set([...baseStaticRoutes, ...blogPageRoutes, ...articleRoutes, ...projectRoutes]))

export default defineNuxtConfig({
  nitro: {
    prerender: {
      crawlLinks: true,              // ????????
      routes: prerenderRoutes,       // ???????????
      autoSubfolderIndex: false,     // ????????? index
      failOnError: false,            // ?????????
      concurrency: 10,               // ????
    },
  },
})
```

#### ?????? Server Plugin??????

????????????????? `server/plugins/prerender.ts`?

```typescript
import { defineNitroPlugin } from 'nitropack/runtime/plugin'
import { portfolio } from '@data/portfolioData.js'

export default defineNitroPlugin((nitroApp) => {
  nitroApp.hooks.hook('prerender:routes', async (ctx) => {
    console.log('[Prerender] Starting route generation...')
    
    // ?? Blog ????
    const POSTS_PER_PAGE = 10
    const articleCount = 6 // ????????
    const totalPages = Math.ceil(articleCount / POSTS_PER_PAGE)
    
    console.log(`[Prerender] Generating ${totalPages} blog pages...`)
    for (let i = 1; i <= totalPages; i++) {
      ctx.routes.add(`/blog/page/${i}`)
    }
    
    // ??????
    const articleSlugs = ['art-nouveau', 'mbe', 'modern-design-intro', 'pixel-art', 'pop-art', 'vaporwave']
    console.log(`[Prerender] Generating ${articleSlugs.length} article pages...`)
    for (const slug of articleSlugs) {
      ctx.routes.add(`/article/${slug}`)
    }
    
    // ??????
    console.log(`[Prerender] Generating ${portfolio.length} project pages...`)
    for (const work of portfolio) {
      ctx.routes.add(`/project/${work.id}`)
    }
    
    console.log('[Prerender] Route generation completed!')
  })
})
```

---

### 3. ??????

? `nuxt.config.ts` ????????

```typescript
routeRules: {
  // ?? - ????
  '/': { prerender: true },

  // ????
  '/about': { prerender: true },
  '/service': { prerender: true },
  '/contact': { prerender: true },
  '/portfolio': { prerender: true },

  // Blog ?? - ????
  '/blog': { redirect: '/blog/page/1' },  // ????????
  '/blog/page/**': { prerender: true },

  // ???? - ????
  '/article/**': { 
    ssr: true,       // ?? SSR
    prerender: true  // ???
  },

  // ???? - ????
  '/project/**': { 
    ssr: true,
    prerender: true 
  },

  // API ?? - ????
  '/api/**': { cors: true },
}
```

**?????**
- `ssr: true` ?????????????
- `prerender: true` ?? Nuxt ????? HTML ? payload
- API ???????????? Nitro Functions

---

### 4. ?? Cloudflare Pages ??

? `nuxt.config.ts` ? `nitro.cloudflare` ????

```typescript
nitro: {
  preset: 'cloudflare-pages',
  cloudflare: {
    deployConfig: true,
    nodeCompat: true,
    routes: {
      include: ['/*'],                    // ??????
      exclude: [
        '/_nuxt/*',                       // Nuxt ??
        '/fonts/*',                       // ????
        '/images/*',                      // ????
      ],
    },
  },
}
```

**???**
- `exclude` ?**??**?? `/api/*`?? API ??? Cloudflare Workers ??
- ?????? API??? Cloudflare Pages ? Dashboard ???????

---

### 5. ??????????

#### ???? (`app/pages/article/[id].vue`)

```vue
<script setup>
const route = useRoute()
const articleId = route.params.id

// ? ????? useAsyncData ??? server: true
const { data: article } = await useAsyncData(
  `article-${articleId}`,
  () => queryContent('articles', articleId).findOne(),
  {
    server: true,  // ? ??? SSG ???
    lazy: false,   // ? ?????
  }
)

// 404 ??
if (!article.value) {
  throw createError({
    statusCode: 404,
    message: '?????'
  })
}
</script>
```

**????? `server: true`?**
- ?????????SSG??????
- ????????????????????? payload ??

#### ???? (`app/pages/project/[id].vue`)

```vue
<script setup>
const route = useRoute()
const projectId = computed(() => route.params.id)

// ? ???? onMounted???? setup ?????
const { getWorkById } = usePortfolio()
const project = ref(getWorkById(projectId.value))

// SSG ?? 404 ??
if (!project.value && import.meta.server) {
  throw createError({ 
    statusCode: 404,
    message: '?????' 
  })
}
</script>
```

**???**
- ??? `onMounted` ??????????????
- ?? `<script setup>` ??????? SSR/SSG ????

---

## ?? ????

### 1. ?? SSG ??

```bash
npm run generate
```

### 2. ??????

```bash
# ?? Blog ??
ls -la output/public/blog/page/
# ????: 1/, 1.html

# ??????
ls -la output/public/article/
# ????: art-nouveau/, art-nouveau.html, mbe/, mbe.html, ...

# ??????
ls -la output/public/project/
# ????: 1/, 1.html, 2/, 2.html, ...

# ?? Payload ??
find output/public -name "_payload.json" | head -20
# ?????? _payload.json ??
```

### 3. ????

```bash
npm run preview
```

?????????
- http://localhost:3000/blog/page/1
- http://localhost:3000/article/art-nouveau
- http://localhost:3000/project/1

**?????**
- ? ????????
- ? ?????F5??????
- ? Network ????? `_payload.json` ?? 200 ? 304
- ? ???? 404 ? JSON ????

### 4. ?? _routes.json

```bash
cat output/_routes.json
```

?? API ????????
```json
{
  "version": 1,
  "include": ["/*"],
  "exclude": ["/_nuxt/*", "/fonts/*", "/images/*"]
}
```

---

## ?? ??? Cloudflare Pages

### ?????? Wrangler CLI

```bash
# ??
npm run generate

# ??
npx wrangler pages deploy output/public --project-name=hodes-nuxt
```

### ????Git ????

??? GitHub ??Cloudflare Pages ???????????

**Cloudflare Pages ?????**
- **????**: `npm run generate`
- **????**: `output/public`
- **Node ??**: 18 ???

---

## ?? ??????

?????????????????

```
? Generated public output/public

???????
  ? /                          (??)
  ? /about                     (??)
  ? /service                   (??)
  ? /contact                   (??)
  ? /portfolio                 (???)
  ? /blog/page/1               (Blog ?1?)
  ? /article/art-nouveau       (??: ???)
  ? /article/mbe               (??: MBE)
  ? /article/modern-design-intro (??: ????)
  ? /article/pixel-art         (??: ????)
  ? /article/pop-art           (??: ????)
  ? /article/vaporwave         (??: ???)
  ? /project/1                 (?? #1)
  ? /project/2                 (?? #2)
  ... (? 90+ ?????)

??: 100+ ???
```

---

## ?? ????

### ?? 1: Payload 404 ??

**???**
```
GET /article/xxx/_payload.json 404 (Not Found)
```

**???**
1. ?? `experimental.payloadExtraction` ??? `true`
2. ???????? `nitro.prerender.routes` ?
3. ????? `useAsyncData` ???? `server: true`
4. ???? `npm run generate`

---

### ?? 2: ???????

**???**
- ???????
- ??????????

**???**
1. ????????? `onMounted` ?????? `<script setup>` ???
2. ????? `useAsyncData` ? `useFetch`??????? API
3. ??????????? `ssr: true`

---

### ?? 3: API ?? 404

**???**
```
GET /api/proxy-image?url=... 404 (Not Found)
```

**???**
1. ?? `nitro.cloudflare.routes.exclude` ?**??** `/api/*`
2. ?? API ??????? `server/api/` ??
3. ?? Cloudflare Pages ? Functions ??????

---

### ?? 4: ????????

**???**
```
Error: Cannot find module '@data/portfolioData.js'
```

**???**
1. ????????????? `nuxt.config.ts` ? `alias` ?
2. ???????????
3. ???????`rm -rf node_modules && npm install`

---

## ?? ????

### ????
- [Nuxt 4 Prerendering](https://nuxt.com/docs/getting-started/prerendering)
- [Nuxt 4 Data Fetching](https://nuxt.com/docs/getting-started/data-fetching)
- [Nuxt Content v3](https://content.nuxt.com/)
- [Cloudflare Pages](https://developers.cloudflare.com/pages/)

### ????
- [SSG_OPTIMIZATION_PLAN.md](./SSG_OPTIMIZATION_PLAN.md) - ??? SSG ????
- [DEPLOYMENT_CHECKLIST.md](./DEPLOYMENT_CHECKLIST.md) - ??????
- [SEO_SETUP.md](./SEO_SETUP.md) - SEO ????

---

## ? ????

??????

- [ ] `experimental.payloadExtraction` ???
- [ ] ?????????? `nitro.prerender.routes`
- [ ] ???? `useAsyncData` ??? `server: true`
- [ ] ??????? `ssr: true` ? `prerender: true`
- [ ] ?? `npm run generate` ??
- [ ] `output/public` ????????? HTML ? `_payload.json` ??
- [ ] ?????????`npm run preview`?
- [ ] ?????????????
- [ ] Network ??? `_payload.json` ????
- [ ] API ??????

---

## ?? ????

### v2.0.0 (2025-10-31)
- ?? ??????????????
- ? ?????????????
- ? ????????????????
- ? ?????? Nuxt 4 ????

### v1.0.0 (2025-10-30)
- ????????????????

---

**????**: Claude (Anthropic AI)  
**?????**: Homer Shie  
**????**: 2025-10-31
