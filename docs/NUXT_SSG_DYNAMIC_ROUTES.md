## Nuxt ????????????

### ??
- ???????? `_preload` ?????????? `_payload.json` ??? chunk 404??? `/blog/page/*`?`/article/*` ????????
- ??? `/project/*` ??? Cloudflare Pages `_routes.json` ?? `/api/*` ????? `api/proxy-image`??????? 404?

### ????
1. `nuxt.config.ts` ? `nitro.prerender.routes` ??? `/`?????????????????SSG ??????? payload?
2. Cloudflare Pages `routes.exclude` ?? `/api/*`????? `_routes.json` ???????

### ????
1. **?????????**
   - ? `nuxt.config.ts` ??? `readdirSync` ? `portfolioData.js`??? `content/articles/*.md` ???????????
   - ???????? `blog/page/<n>` ?????????????????? `nitro.prerender.routes`?
   - ?? `crawlLinks: true`??? Nuxt ????????????

   ```ts
   const articleSlugs = getArticleSlugs()
   const blogPageRoutes = Array.from({ length: Math.max(1, Math.ceil(articleSlugs.length / 10)) }, (_, i) => `/blog/page/${i + 1}`)
   const projectRoutes = resolvePortfolioRoutes()
   const prerenderRoutes = Array.from(new Set([...baseStaticRoutes, ...blogPageRoutes, ...articleRoutes, ...projectRoutes]))

   export default defineNuxtConfig({
     nitro: {
       prerender: {
         routes: prerenderRoutes,
       },
     },
   })
   ```

2. **?? Cloudflare Routes**
   - ?? `include: ['/*']`???? `exclude: ['/api/*', ...]` ?? `/api/*`?? `api/proxy-image` ?? Nitro Functions ???

   ```ts
   cloudflare: {
     routes: {
       include: ['/*'],
       exclude: ['/_nuxt/*', '/fonts/*', '/images/*'],
     },
   }
   ```

### ????
1. ???? `npm run generate`??? `output/public` ?????
   - `/article/<slug>/_payload.json` ? `.html`
   - `/blog/page/<n>/_payload.json`
   - `/project/<id>/_payload.json`
2. ?? `output/_routes.json`??? `/api/*` ?????
3. ??? Cloudflare Pages ???????
   - `https://www.homershie.com/blog/page/1`
   - `https://www.homershie.com/article/<slug>`
   - `https://www.homershie.com/project/<id>`
   - `https://www.homershie.com/api/proxy-image?...`

### ????
- ??? Markdown ????????`npm run generate` ???????????????????????
- ????????????????? CDN ??????? `_payload.json` ????
- ???????????? `portfolioData` ????????????????? `nuxt.config.ts` ?? client-only ???
