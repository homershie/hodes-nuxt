# SSG ??????

> **????**: 2025-10-31  
> **??**: ?? Nuxt SSG ??? Cloudflare Pages ??????

---

## ?? ????

??? Cloudflare Pages ?????????

1. **Icon API 404**: `GET /api/_nuxt_icon/mdi.json 404`
2. **Proxy Image 404**: `GET /api/proxy-image?url=... 404`
3. **CORS ??**: R2 bucket ??? HEAD ?????
4. **Payload 404**: `GET /article/xxx/_payload.json 404`
5. **????????????**

---

## ? ????

### 1. ????? Cloudflare routes ??

**??**: `nuxt.config.ts`

**??**: `nitro.cloudflare.routes` ?????? Nuxt 4?????????

**??**:
```typescript
// ? ?????? TypeScript ???
cloudflare: {
  deployConfig: true,
  nodeCompat: true,
  routes: {
    include: ['/*'],
    exclude: ['/_nuxt/*', '/fonts/*', '/images/*'],
  },
}

// ? ??
cloudflare: {
  deployConfig: true,
  nodeCompat: true,
}
```

---

### 2. ?? Nuxt Icon ? SSG ??????

**??**: `nuxt.config.ts`

**??**: ?? `provider: 'server'` ? SSG ?????? 404??????????? server?

**??**:
```typescript
// ? ???SSG ???? 404?
icon: {
  provider: 'server',
  serverBundle: { collections: ['mdi'] },
  clientBundle: {
    scan: true,
    icons: ['mdi:email-outline', 'mdi:dumbbell', ...],
  },
}

// ? ????? Iconify CDN ????????
icon: {
  // ????? iconify provider
  clientBundle: {
    scan: true,
    sizeLimitKb: 256,
  },
}
```

**??**:
- Icons ?? Iconify CDN ??????????????
- ???? `/api/_nuxt_icon/` API ??

---

### 3. ????? CORS ? HEAD ??

**??**: `app/pages/project/[id].vue`

**??**: ?? `getBestImagePath()` ?? HEAD ????????????? R2 bucket ? CORS ???

**??**:
```javascript
// ? ?????? CORS ???
const processedImages = await Promise.all(
  images.map(async img => await getBestImagePath(img))
)

// ? ??????? WebP ???
const webpImages = images.map(img => 
  img.replace(/\.(jpg|png)$/i, '.webp')
)
```

**??**:
- ???? HEAD ??????
- ???? WebP ???????????????

---

### 4. ?? Nuxt Content API ????

**??**: `app/pages/article/[id].vue`

**??**: ????? API (`queryCollection`) ??????

**??**:
```typescript
// ? ??
const { data } = await useAsyncData(
  `article-${articleId}`,
  () => queryCollection('content').all()
)
// ????: item.stem, item.meta, item.path

// ? ??
const { data } = await useAsyncData(
  'articles-list',
  () => queryContent('articles').find(),
  { server: true, lazy: false }
)
// ????: item._path, item.title, item.date
```

**????**:
- ?? `queryContent()` ??? `queryCollection()`
- ?? `_path` ??? `path`
- ???? frontmatter ?????? `meta` ??

---

## ?? ????

### ????

```bash
# 1. ?? SSG ??
npm run generate

# 2. ????
ls -la output/public/article/
ls -la output/public/project/

# 3. ??
npm run preview

# 4. ????
# - ?? http://localhost:3000/about
# - ?? http://localhost:3000/article/art-nouveau
# - ?? http://localhost:3000/project/1
# - ???????F5???????
```

### ?????

1. ? About?Contact?Service ??? icons ????
2. ? Project ?????????? CORS ??
3. ? Article ????????
4. ? ???????????????
5. ? Console ? 404 ? CORS ??

---

## ?? ??????

1. **SSG ??????? server provider**
   - Icons?API ????? CDN ???????
   - ???????? server ??

2. **??????????**
   - ???????? HEAD ??????
   - ???????????????

3. **????? Nuxt Content API**
   - ?? `queryContent()` ????
   - ??????????? `_path`?
   - ? `useAsyncData` ??? `server: true` ? `lazy: false`

4. **Cloudflare Pages ?????**
   - ?????? `routes`?? Nuxt ????
   - ?????????`deployConfig` ? `nodeCompat`

---

## ?? ????

- [NUXT_SSG_DYNAMIC_ROUTES.md](./NUXT_SSG_DYNAMIC_ROUTES.md) - ??? SSG ????
- [nuxt.config.ts](../nuxt.config.ts) - Nuxt ???
- [app/pages/article/[id].vue](../app/pages/article/[id].vue) - ????
- [app/pages/project/[id].vue](../app/pages/project/[id].vue) - ????

---

**??????**: 2025-10-31
