# 404 ??????

## ?? ????

### ????????
1. **?? Payload ??** ? ? ????????
2. **??**?Nuxt 4 ? SSG ?? `_payload.json` ???????
3. **?????**??? payload ???????????????????

### ????
```
GET /article/art-nouveau/_payload.json 404 (Not Found)
GET /project/89/_payload.json 404 (Not Found)
[nuxt] Cannot load payload SyntaxError: Unexpected end of JSON input
```

## ? ???????

### 1. ?? Payload ????
```typescript
experimental: {
  componentIslands: true,
  payloadExtraction: true, // ? ?????
}
```

**??**?
- Nuxt 4 ? SSG ?? payload ?????????
- ?????????????????
- ????????

### 2. ???????

#### ???????
```typescript
prerender: {
  crawlLinks: true,
  routes: ['/'], // ??????
  concurrency: 10, // ?????
}
```

**??**?
- ? `server/plugins/prerender.ts` ????????
- ??????????
- 90 ???????????

#### ?????????
```typescript
routeRules: {
  '/article/**': { 
    ssr: true,      // ?? SSR
    prerender: true // ?????
  },
  '/project/**': { 
    ssr: true, 
    prerender: true 
  },
}
```

**??**?
- `ssr: true` ?????????????
- `prerender: true` ?????? HTML ? payload
- ????????

### 3. ??????

#### ???? (article/[id].vue)
```typescript
const { data: allArticles } = await useAsyncData(
  `article-${articleId}`, 
  () => queryCollection('content').all(),
  {
    server: true,  // ? ? SSR/SSG ???
    lazy: false,   // ? ?????
  }
)
```

**??**?
- ?? `server: true` ??
- ?? `lazy: false` ??
- ?????????????? payload

#### ???? (project/[id].vue)
```typescript
// ? ????? onMounted ????????
onMounted(() => {
  project.value = getWorkById(projectId)
})

// ? ??????????????
project.value = getWorkById(projectId.value)

if (!project.value && import.meta.server) {
  throw createError({ statusCode: 404 })
}
```

**??**?
- ? `<script setup>` ????????
- ?? SSG ?????
- ???????? 404

## ?? ????

### 1. ?????
```bash
./test-build.sh
```

### 2. ??????
????????
- ? 6 ?????
- ? 90 ?????
- ? Blog ??
- ? Payload ??

### 3. ????
```bash
npm run preview
```

??????
1. http://localhost:3000/article/art-nouveau
2. http://localhost:3000/project/89
3. http://localhost:3000/blog/page/1

**????**?
- ? ????????????
- ? ? F5 ?????? 404
- ? ????? payload ??
- ? ??????

### 4. ???????
?? Network ??????
- `_payload.json` ???????200?
- ??? 304???????????
- ????? 404

## ?? ????

### ???
```
[Prerender] Starting route generation...
[Prerender] Generating 1 blog pages...
[Prerender] Added: /blog/page/1
[Prerender] Generating 6 article pages...
[Prerender] Added: /article/art-nouveau
[Prerender] Added: /article/mbe
...
[Prerender] Generating 90 project pages...
[Prerender] Added: /project/1
[Prerender] Added: /project/2
...
[Prerender] Route generation completed successfully!
```

### ???
- ? ?????????
- ? ?????????
- ? ????????
- ? ??????
- ? ?? 404 ??

## ?? ??

????????????

```bash
git add .
git commit -m "fix: resolve SSG payload issues for dynamic routes

Key changes:
- Keep payloadExtraction enabled (required for SSG)
- Add server:true and lazy:false to useAsyncData
- Fix project page data fetching in SSG
- Optimize prerender configuration
- Ensure all 90 project routes are generated

This fixes:
- Article content disappearing after refresh
- Payload 404 errors
- Project pages not found
- Blog pages showing errors

All dynamic routes now work correctly with proper SSG."

git push
```

## ?? ????

1. **???? payloadExtraction**
   - ?? Nuxt 4 SSG ?????
   - ?????????

2. **useAsyncData ???? server:true**
   - ??? SSG ???
   - ??????? payload

3. **?????? ssr:true + prerender:true**
   - ??????
   - ???????????

4. **???????? onMounted**
   - onMounted ???????
   - SSG ??????????

## ?? ????

- [Nuxt 4 Data Fetching](https://nuxt.com/docs/getting-started/data-fetching)
- [Nuxt 4 SSG](https://nuxt.com/docs/getting-started/prerendering)
- [Nuxt Content v3](https://content.nuxt.com/)
